import 'package:flutter/material.dart';

class CrownImage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Positioned(
        top: 0, right: 0, child: Image.asset("images/crown2.png"));
  }
}
